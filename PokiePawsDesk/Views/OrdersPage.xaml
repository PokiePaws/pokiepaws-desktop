<Page x:Class="PokiePawsDesk.Views.OrdersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:PokiePawsDesk.Views"
      Title="Zamówienia" Background="#F5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#1A237E">
            <TextBlock Text="Zamówienia" FontSize="20" FontWeight="Bold"
                       Foreground="White" VerticalAlignment="Center" Margin="24,0"/>
        </Grid>

        <DataGrid Grid.Row="1" x:Name="OrdersGrid" Margin="16"
                  AutoGenerateColumns="False" IsReadOnly="False"
                  Background="White" BorderThickness="1" BorderBrush="#DDD"
                  RowBackground="White" AlternatingRowBackground="#F9F9F9"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  SelectionMode="Single" CanUserAddRows="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" IsReadOnly="True"/>
                <DataGridTextColumn Header="Gabinet" Binding="{Binding ClinicName}" Width="200" IsReadOnly="True"/>
                <DataGridTemplateColumn Header="Status" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Status}" VerticalAlignment="Center" Padding="4,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ComboBox SelectedItem="{Binding Status, UpdateSourceTrigger=PropertyChanged}"
                                      ItemsSource="{x:Static local:OrdersPage.Statuses}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Data" Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Wartość" Binding="{Binding TotalPrice, StringFormat={}{0:N2} zł}" Width="120" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>